{
  "name": "ts-unused",
  "version": "2.0.1",
  "description": "Find unused exports and properties in TypeScript projects",
  "license": "MIT",
  "bin": {
    "ts-unused": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build:cli": "bun build src/cli.ts --outdir dist --target node --external ts-morph --external typescript --minify --sourcemap=external",
    "build:lib": "tsgo --project tsconfig.build.json",
    "build": "bun clean && bun build:cli && bun build:lib",
    "typecheck": "tsgo --noEmit",
    "lint": "bun run fix && biome check src",
    "fix": "biome check --write --unsafe src",
    "sanity-check": "bun run scripts/sanity-check.ts",
    "prepublish": "bun lint && bun test && bun run build && bun sanity-check --no-build",
    "cli": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.0",
  },
  "dependencies": {
    "ts-morph": "^28.0.0",
    "typescript": "^7.0.1-rc"
  }
}
